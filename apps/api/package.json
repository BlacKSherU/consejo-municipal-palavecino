{
  "name": "@cmp/api",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:dry-run": "wrangler deploy --dry-run",
    "db:migrate:local": "wrangler d1 migrations apply cmp-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply cmp-db --remote",
    "seed": "node scripts/seed-admin.mjs",
    "seed:demo": "node scripts/seed-demo.mjs --write"
  },
  "dependencies": {
    "hono": "^4.6.14",
    "jose": "^5.9.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "typescript": "^5.7.2",
    "wrangler": "^3.99.0"
  }
}
