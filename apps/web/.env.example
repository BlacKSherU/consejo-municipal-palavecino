# En Cloudflare Pages (Production): añade PUBLIC_API_URL = https://tu-worker.workers.dev (sin / final).
# En local suele ir vacío: el proxy de Vite reenvía /api al Worker (astro.config.mjs).
PUBLIC_API_URL=
