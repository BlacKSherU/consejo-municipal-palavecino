# Vacío en desarrollo: las peticiones a /api se reenvían al Worker (ver astro.config.mjs).
# En producción, si el sitio Pages y el Worker están en dominios distintos:
# PUBLIC_API_URL=https://tu-worker.workers.dev
