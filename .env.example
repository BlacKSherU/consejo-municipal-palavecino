# Copy to .env and set real values (never commit .env).
# Wire these in settings when moving secrets out of the repo.

# DJANGO_SECRET_KEY=
# DJANGO_DEBUG=True
# DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1
