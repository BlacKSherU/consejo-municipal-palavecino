# Copy to .env on the server (never commit .env).
# settings.py carga este archivo con python-dotenv (tras: pip install -r requirements.txt).
# Las líneas de aquí tienen prioridad sobre Environment= de systemd (override=True).
# Valores booleanos: True/true/1/yes/on (con o sin comillas). Una variable por línea, sin espacio antes del =.

# Variables que SÍ lee Django (prefijo DJANGO_):
# DJANGO_SECRET_KEY=
DJANGO_DEBUG=True
# DJANGO_ALLOWED_HOSTS=tudominio.com,www.tudominio.com
# Con DJANGO_DEBUG=False, obligatorio: cada Host de la petición debe estar en esta lista (o 400).
# Con DJANGO_DEBUG=True, Django usa ALLOWED_HOSTS=['*'] (cualquier Host); no dejes DEBUG=True en internet.
# Tras nginx con proxy_pass, si sigues con 400 y DEBUG=False, prueba:
# DJANGO_USE_X_FORWARDED_HOST=True

# HTTPS terminado en nginx (Let's Encrypt, etc.): nginx debe pasar X-Forwarded-Proto https
# DJANGO_SECURE_SSL=True

# El puerto NO es un setting de Django: no pongas PORT=8000 esperando que runserver/gunicorn
# lo lean desde aquí. Usa la línea de comando, p. ej.:
#   python manage.py runserver 0.0.0.0:8001
#   gunicorn ... --bind 127.0.0.1:8000
# O define PORT en systemd/supervisor y úsalo en ExecStart (--bind 127.0.0.1:$PORT).

# --- Logging (servidor): DJANGO_LOG_ROOT_LEVEL y DJANGO_LOG_DJANGO_LEVEL = DEBUG, INFO, WARNING, ERROR, CRITICAL
# DJANGO_LOG_ROOT_LEVEL=
# DJANGO_LOG_DJANGO_LEVEL=
# DJANGO_LOG_SQL=True
# DJANGO_LOG_REQUEST=True
# DJANGO_LOG_SERVER=False
# DJANGO_LOG_SECURITY=True
# DJANGO_LOG_CORE=True
# Consola del navegador (usa cmpDebug.log en JS, no console.log suelto)
# DJANGO_BROWSER_CONSOLE_LOG=True
